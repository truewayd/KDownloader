{
  "name": "kdownloader",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pwsh -NoProfile -ExecutionPolicy Bypass -File tools/build-extension.ps1",
    "test": "node --test \"tests/*.test.mjs\"",
    "test:python": "python -m unittest tests/migrate_history_json_test.py",
    "version:check": "pwsh -NoProfile -ExecutionPolicy Bypass -File tools/read-extension-version.ps1"
  }
}
