# TrueDown reduced outlines

- Remove decorative outlines from panels, buttons, module/update cards and download option containers. Use existing surface fills and spacing to separate content.
- Remove the selected navigation item's enclosing outline while retaining its accent marker.
- Preserve input boundaries, keyboard focus indicators, native window frames and forced-color button boundaries.

No configuration or storage changes.

## Verification

Verified with the workspace, settings, auxiliary UI and task-form browser acceptance suites in light and dark themes.
