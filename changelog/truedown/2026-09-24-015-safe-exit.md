# Faster safe exit

- Request normal aria2 shutdown through an OS interrupt, avoiding the RPC's fixed three-second delay while retaining checkpoint and state flushing.
- On Windows, use a bounded, hidden helper that signals only the engine's private console. Keep the core's console handlers and crash-cleanup job intact; retain RPC fallback and bounded process cleanup.
- Preserve NEXT 2.8.2 HTTP range checkpoints in its native state directory and omit its retired `always-resume` option.

## Verification

- Go suite and vet; Windows native context-menu authorization, label, icon and owner-drawing tests.
- Stable aria2: RPC shutdown 4.01s versus normal interrupt 1.01s. Active-download shutdown 1.01s, then resume from saved ranges with exact final file contents.
- NEXT 2.8.2: active-download shutdown 0.17s and exact resumed output. Immediate exit during cold startup still varies with native network cleanup; do not shorten its safe cleanup budget.
- Packaged core: standalone, desktop pipe EOF, HTTP exit and private-pipe exit finish in 0.51-0.72s; attachment preserves the independent service.
- Native menu tests do not establish live pointer/display alignment; interactive user acceptance remains necessary.
