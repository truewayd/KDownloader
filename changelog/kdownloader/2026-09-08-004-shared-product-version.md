# KDownloader 1.6.0

- Advance the shared product version alongside TrueDown's corrected native title area and file-group output directories.
- Extension requests retain their selected root and creator hierarchy; TrueDown now places newly created files under the appropriate group subdirectory within that root. Existing TrueDown tasks retain their original paths.

## Verification

- `npm test`, `npm run ui:check` and `npm run version:check`.
