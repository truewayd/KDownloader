# Shared product version 1.5.0

Advance the shared manifest version for TrueDown's settings, proxy and native
update changes. Extension behavior remains unchanged from the dependency update.

## Verification

The repository Node suite and generated shared UI/icon checks pass. TrueDown
preparation reads the same product version for the shell and both Go sidecars.
