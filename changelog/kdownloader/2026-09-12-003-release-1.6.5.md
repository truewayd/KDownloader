# KDownloader 1.6.5 - Cache and configuration consistency

- Wait for pending creator-cache writes, and reject old fetch/XHR completions
  after page cleanup or bfcache restoration.
- Preserve valid creator flags whose tuple keys expand during JSON escaping.
- Preserve newer Gist settings when an earlier upload finishes after the token,
  selected Gist, or enabled state changes, including restoring defaults.
- Advance the shared product version to 1.6.5 for these fixes and the TrueDown
  authentication, output ownership, and UI consistency repairs.

## Verification

Lifecycle, history, configuration, Gist, and shared UI regressions pass. Gist
tests use mocked network responses. The clean extension build, version check,
history migration tests, and product changelog selection are part of the audit
verification recorded in `docs/project-audit-2026-09-12.md`.

This pass does not establish authenticated live-site behavior or real Chrome
action-popup appearance; extension UI styling and popup width are unchanged.
